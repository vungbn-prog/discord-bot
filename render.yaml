services:
  - type: web
    name: discord-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: DISCORD_TOKEN
        value: ${DISCORD_TOKEN}
build:
  environment:
    pythonVersion: 3.11.6
